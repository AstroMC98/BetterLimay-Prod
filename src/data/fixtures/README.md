# Contract fixtures

These fixtures exercise schema behavior only. They are synthetic and must never be imported into production routes or presented as Limay facts.

Each dataset has one `verified`, one `unverified`, and one intentionally `invalid` record. The verified records are structurally valid synthetic examples, not verified public information.

