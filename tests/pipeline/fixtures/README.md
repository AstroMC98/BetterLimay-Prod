# Pipeline fixtures

These are synthetic fixtures only; they contain no Limay or other LGU records.
The text-PDF test creates a small selectable PDF in a temporary workspace with
ReportLab, while the scanned-PDF test injects a deterministic OCR runner so the
suite never depends on a Tesseract binary or a real government document.
